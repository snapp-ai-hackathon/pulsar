# Pulsar — ارائه ساده و کامل (۱۰ دقیقه)

## 📚 توضیح اصطلاحات فنی

**MLOps**: مدیریت چرخه‌ی کامل مدل‌های ML از آموزش تا استقرار. مثل DevOps برای نرم‌افزار.

**MLflow**: ابزار مدیریت مدل‌های ML. رجیستری یعنی دیتابیس مرکزی که همه‌ی مدل‌ها را نگه می‌دارد. Traceability یعنی می‌توانیم ببینیم هر مدل با چه پارامترهایی آموزش دیده.

**NATS**: سیستم messaging. Event-driven یعنی وقتی یک event اتفاق می‌افتد، مدل retrain می‌شود.

**ElasticNet**: مدل ML خطی قابل توضیح. می‌توانیم ببینیم هر feature چقدر تأثیر دارد.

**Temporal CNN**: شبکه‌ی عصبی برای تشخیص الگوهای زمانی. یک پنجره‌ی زمانی می‌گیرد و الگوهای پیچیده را شناسایی می‌کند.

**MAE**: میانگین خطای مطلق. یعنی به‌طور متوسط چقدر خطا داریم.

**RMSE**: جذر میانگین مربعات خطا. به خطاهای بزرگ وزن بیشتری می‌دهد.

**Parquet**: فرمت فایل برای ذخیره‌سازی داده‌های ستونی. سریع‌تر از CSV.

**ClickHouse**: دیتابیس analytics برای query‌های سریع روی داده‌های بزرگ.

---

## 🎯 ارائه Pulsar — متن کامل برای خواندن

### معرفی

سلام. من امروز می‌خواهم درباره Pulsar صحبت کنم — یک سیستم پیش‌بینی سرج که برای سه گروه مختلف ساخته شده: راننده‌ها، مسافرها، و تیم‌های عملیاتی.

### مسئله: چرا Pulsar ساخته شد؟

**برای راننده‌ها**: راننده‌ها نمی‌دانند کجا بروند تا درآمد بهینه داشته باشند. زمان مُرده دارند و سرج‌ها غیرقابل‌پیش‌بینی هستند.

**برای مسافرها**: مسافرها هر بار اپ را باز می‌کنند با سرج عجیب روبه‌رو می‌شوند. تجربه‌ی کاربری ناپایدار است.

**برای تیم‌های عملیاتی**: تیم‌های ops مجبورند مدام دستی آتش‌نشانی کنند — قیمت را بالا/پایین ببرند، کمپین بزنند، نقشه را تنظیم کنند. همه چیز واکنشی است، نه پیش‌کنشی.

### راه‌حل: Pulsar

Pulsar یک موتور پیش‌بینی چندافقی است که روی شبکه هگزاگونی شهر سوار می‌شود و قبل از وقوع بحران سیگنال می‌دهد.

**برای راننده**: Pulsar به راننده می‌گوید کجا برود که تقاضا بیشتر است. مثلاً می‌گوید: «در ۳۰ دقیقه‌ی بعدی در این محدوده سرج +۲۲% می‌رود، الان حرکت کن.» این باعث می‌شود راننده درآمد بهتری داشته باشد و زمان مُرده نداشته باشد.

**برای مسافر**: Pulsar به مسافر می‌گوید به جایی برود که راننده‌های فعال بیشتر هستند. در محصول، ما وضعیت راننده‌های فعال را به صورت real-time نشان می‌دهیم — مسافر می‌تواند ببیند کجا راننده‌های فعال بیشتری هستند و به آن سمت برود. این باعث می‌شود مسافر سریع‌تر سوار شود و تجربه‌ی بهتری داشته باشد.

**برای تیم عملیاتی**: Pulsar به ops می‌گوید کجا کمپین بگذارد و کجا نگذارد. مثلاً می‌گوید: «این منطقه در ۶۰ دقیقه‌ی آینده oversupply خواهد داشت، کمپین را این‌جا فعال نکن.» این باعث می‌شود ops تصمیم‌های بهتری بگیرد و کمتر آتش‌نشانی کند.

### چرا Pulsar متفاوت است؟

در حالی که بسیاری از راه‌حل‌ها روی یک جنبه تمرکز می‌کنند — مثلاً فقط مدل ML یا فقط داشبورد — Pulsar سه لایه‌ی به‌هم‌پیوسته را یکجا ارائه می‌دهد.

**لایه اول: Feature Engineering زمان‑سری**

ما خودمان لایه‌ی داده را می‌سازیم. داده‌های واقعی از ClickHouse — جدول kandoo_parameter_nats — به صورت CSV می‌آید. این داده‌ها را به صورت خودکار به دیتاست‌های زمان‑سری تبدیل می‌کنیم. lag features می‌سازیم — مثلاً demand یک بازه قبل. cumulative signals می‌سازیم — مثلاً surge تجمیعی. پنجره‌های زمانی می‌سازیم. همه این‌ها در Parquet ذخیره می‌شود برای دسترسی سریع.

**لایه دوم: مدل‌های چندگانه**

ما دو مدل داریم. ElasticNet — یک مدل سریع و قابل توضیح. می‌توانیم ببینیم هر feature چقدر تأثیر دارد. و Temporal CNN — یک شبکه‌ی عصبی عمیق که یک پنجره‌ی زمانی از ۱۲ بازه‌ی اخیر را می‌گیرد و الگوهای پیچیده را شناسایی می‌کند. این مدل می‌تواند پیش‌بینی کند که در ۳۰، ۶۰، و ۹۰ دقیقه‌ی آینده چه اتفاقی می‌افتد.

**لایه سوم: زیرساخت MLOps**

از روز اول با MLflow کار می‌کنیم. هر مدل با یک URI ثبت می‌شود. می‌توانیم ببینیم هر مدل با چه پارامترهایی آموزش دیده و چه نتیجه‌ای داشته. با NATS کار می‌کنیم — event-driven. یعنی وقتی یک event اتفاق می‌افتد، مدل retrain می‌شود. نه cron job.

### Production-Ready از روز اول

این فقط یک PoC نیست. مدل‌ها روی داده‌های واقعی ۸ روزه آموزش دیده‌اند. آرتیفکت‌های MLflow در ریپو موجود است — می‌توانید ببینید. Pipeline کامل از CSV تا پیش‌بینی در یک CLI واحد. کانتینری‌سازی شده با Docker و Helm charts آماده برای Kubernetes.

### عملکرد مدل‌ها

MAE در حدود ۱۰۰ تا ۱۳۰ است. RMSE در حدود ۱۴۰ تا ۱۷۰ است. demand signal در scale ۰ تا ۱۰۰۰ است. این یعنی به‌طور متوسط خطای مطلق حدود ۱۰ تا ۱۳ درصد است (۱۰۰ از ۱۰۰۰ = ۱۰٪). این برای یک مدل اولیه قابل قبول است و ما از این عدد دفاع می‌کنیم — مدل ما بهتر از baseline عمل می‌کند و برای use case ما که جهت پیش‌بینی مهم‌تر از دقت مطلق است، این مقدار کافی است. هدف ما در production کاهش MAE به زیر ۱۰۰ است.

اما نکته مهم این است: برای use case ما، جهت پیش‌بینی مهم‌تر از مقدار دقیق است. اگر مدل بتواند بگوید «سرج در ۳۰ دقیقه +۲۰% می‌رود»، حتی اگر مقدار دقیق ±۵% خطا داشته باشد، کافی است.

### معماری داده

داده‌ها از ClickHouse می‌آیند — جدول kandoo_parameter_nats. این داده‌ها به صورت CSV export می‌شوند. ما این داده‌ها را می‌خوانیم و برای هر hexagon و service type یک snapshot می‌سازیم. این snapshot‌ها را در Parquet ذخیره می‌کنیم. برای ElasticNet، lag features می‌سازیم. برای CNN، پنجره‌های زمانی می‌سازیم. مدل‌ها آموزش می‌بینند و در MLflow ثبت می‌شوند.

### NATS — Event-Driven Training

با NATS می‌توانیم آموزش مدل را event-driven کنیم. یک سرویس دیگر یک پیام روی NATS publish می‌کند: «الان مدل را retrain کن». Pulsar این پیام را می‌گیرد و مدل را retrain می‌کند. این یعنی آموزش مدل یک event می‌شود، نه یک job دستی.

### ویژگی‌های آینده — آب و هوا

در آینده می‌خواهیم داده‌های آب و هوا را هم به مدل اضافه کنیم. چطور کار می‌کند؟ وقتی باران می‌بارد یا هوا خیلی سرد است، تقاضا برای سرویس افزایش می‌یابد. ما می‌خواهیم این الگوها را یاد بگیریم و پیش‌بینی کنیم. مثلاً اگر پیش‌بینی آب و هوا نشان دهد که در ۳۰ دقیقه‌ی آینده باران می‌بارد، مدل می‌تواند پیش‌بینی کند که تقاضا در آن منطقه افزایش می‌یابد و به راننده‌ها بگوید قبل از باران به آن منطقه بروند. این باعث می‌شود سرج قبل از وقوع بحران مدیریت شود و هم راننده و هم مسافر تجربه‌ی بهتری داشته باشند.

### تفاوت با راه‌حل‌های دیگر

راه‌حل‌های دیگر معمولاً یک مدل ساده دارند یا فقط PoC هستند. ما Hybrid داریم — ElasticNet + CNN. راه‌حل‌های دیگر روی داده‌های آماده تکیه می‌کنند. ما خودمان لایه‌ی داده را می‌سازیم. راه‌حل‌های دیگر MLOps را بعداً اضافه می‌کنند. ما از روز اول داریم. راه‌حل‌های دیگر نیاز به تغییرات زیاد دارند. ما از infrastructure موجود اسنپ استفاده می‌کنیم — ClickHouse، NATS، Kubernetes.

---

## 8. سؤالات سخت و انتقادی — آماده‌سازی کامل برای داوری

### سؤال ۱: چرا CNN و نه LSTM/Transformer؟

**پاسخ:** CNN برای پنجره‌های کوتاه — ۱ تا ۲ ساعت — بهینه است. در پیش‌بینی سرج، ما به الگوهای محلی نیاز داریم، نه dependencies طولانی‌مدت. CNN روی GPU در چند دقیقه آموزش می‌بیند. LSTM/Transformer نیاز به ساعت‌ها زمان دارند. برای retraining مکرر این تفاوت حیاتی است. LSTM/TFN در roadmap است، اما اول می‌خواستیم pipeline را validate کنیم.

**نکته کلیدی:** «ما از ساده شروع کردیم تا pipeline را validate کنیم. CNN برای افق ۳۰–۹۰ دقیقه کافی است.»

---

### سؤال ۲: دقت مدل‌های شما چقدر است؟

**پاسخ:** MAE در حدود ۱۰۰–۱۳۰ و RMSE در حدود ۱۴۰–۱۷۰ است. demand signal در scale ۰–۱۰۰۰ است. این یعنی به‌طور متوسط خطای مطلق حدود ۱۰–۱۳ درصد است. این برای یک مدل اولیه قابل قبول است. مدل ما بهتر از baseline عمل می‌کند. هدف ما در production کاهش MAE به زیر ۱۰۰ است.

اما مهم‌تر از دقت مطلق، جهت پیش‌بینی است. اگر مدل بتواند بگوید «سرج در ۳۰ دقیقه +۲۰% می‌رود»، حتی اگر مقدار دقیق ±۵% خطا داشته باشد، کافی است.

**نکته کلیدی:** «ما اول pipeline را ساختیم. دقت را در production با A/B testing اندازه می‌گیریم.»

---

### سؤال ۳: چرا ElasticNet و CNN را جداگانه دارید؟

**پاسخ:** ElasticNet برای validation و explainability است. CNN برای production prediction است. هر کدام use case متفاوتی دارند. Ensemble در roadmap است، اما اول می‌خواستیم هر مدل را جداگانه validate کنیم. ElasticNet به عنوان fallback استفاده می‌شود اگر CNN fail کند.

**نکته کلیدی:** «هر مدل use case متفاوتی دارد. ElasticNet برای explainability، CNN برای accuracy.»

---

### سؤال ۴: معماری شما چقدر scalable است؟

**پاسخ:** هر مدل مستقل است — per city و service type. پس می‌توانیم parallel training کنیم. Parquet برای query سریع طراحی شده. مدل‌های آموزش‌دیده در MLflow cache می‌شوند. API layer stateless است — می‌توانیم replica کنیم.

**نکته کلیدی:** «معماری ما stateless و parallel-ready است. برای scale بزرگ، horizontal scaling داریم.»

---

### سؤال ۵: چرا از ClickHouse و NATS استفاده کردید؟

**پاسخ:** ClickHouse در حال حاضر در استک اسنپ استفاده می‌شود. ما نمی‌خواستیم infrastructure جدید اضافه کنیم. NATS سبک‌وزن و event-driven است. برای retraining triggers کافی است. Kafka برای این use case overkill است.

**نکته کلیدی:** «ما از infrastructure موجود اسنپ استفاده کردیم.»

---

### سؤال ۶: داده‌های شما چقدر کامل است؟

**پاسخ:** در CSVSignalLoader و SnapshotBuilder validation داریم. برای missing data، forward fill و backward fill استفاده می‌کنیم. برای lag features، از mean استفاده می‌کنیم. اگر window کامل نباشد، آن sequence را skip می‌کنیم. در MLflow، تعداد missing values را log می‌کنیم.

**نکته کلیدی:** «ما data validation و missing data handling داریم.»

---

### سؤال ۷: چطور مطمئن می‌شوید داده‌های شما biased نیست؟

**پاسخ:** در MLflow، تعداد hexagons و service types را log می‌کنیم. برای training، از stratified sampling استفاده می‌کنیم. روی subgroups مختلف validate می‌کنیم. Fairness constraints در roadmap است.

**نکته کلیدی:** «ما coverage و fairness را track می‌کنیم.»

---

### سؤال ۸: داده‌های شما چقدر fresh است؟

**پاسخ:** داده‌ها از ClickHouse با cadence ۷.۵ دقیقه می‌آیند. CSV تا TimeSeriesStore: ۱–۲ دقیقه. TimeSeriesStore تا prediction: کمتر از ۱۰۰ میلی‌ثانیه — چون مدل cached است.

**نکته کلیدی:** «داده‌ها هر ۷.۵ دقیقه refresh می‌شوند. Prediction latency کمتر از ۱۰۰ms است.»

---

### سؤال ۹: اگر مدل fail کند چه می‌شود؟

**پاسخ:** ElasticNet به عنوان fallback استفاده می‌شود. اگر confidence score پایین باشد، prediction را reject می‌کنیم. اگر error rate بالا برود، model را disable می‌کنیم. تیم ops می‌تواند manually disable کند.

**نکته کلیدی:** «ما fallback model، confidence threshold، و circuit breaker داریم.»

---

### سؤال ۱۰: چطور model drift را detect می‌کنید؟

**پاسخ:** مدل هر ۶ ساعت retrain می‌شود — از طریق NATS event. اگر MAE/RMSE در production بالا برود، alert می‌دهیم. Drift detection در roadmap است.

**نکته کلیدی:** «ما retraining schedule داریم. Drift detection در roadmap است.»

---

### سؤال ۱۱: Security و privacy چطور؟

**پاسخ:** ما فقط aggregated data استفاده می‌کنیم — hexagon-level، نه driver-level. هیچ PII ذخیره نمی‌شود. API با authentication محافظت می‌شود. در production، data encrypted است.

**نکته کلیدی:** «ما فقط aggregated data استفاده می‌کنیم، نه PII.»

---

### سؤال ۱۲: Cost چقدر است؟

**پاسخ:** Storage overhead کم است — Parquet و tmp/mlruns. Training یک بار انجام می‌شود — هر ۶ ساعت. Serving بسیار سبک است — کمتر از ۱۰۰ms. از infrastructure موجود اسنپ استفاده می‌کنیم — cost اضافی minimal است.

**نکته کلیدی:** «Cost minimal است چون از infrastructure موجود استفاده می‌کنیم.»

---

### سؤال ۱۳: چطور مطمئن می‌شوید واقعاً درآمد را بالا می‌برد؟

**پاسخ:** روی داده‌های historical، پیش‌بینی‌های ما با actual surge مقایسه شده و correlation مثبت دارد. در production، A/B test طراحی کرده‌ایم — control group و treatment group. اول روی یک شهر کوچک test می‌کنیم.

**نکته کلیدی:** «ما pipeline را validate کردیم. Business impact را در A/B test اندازه می‌گیریم.»

---

### سؤال ۱۴: چرا راننده‌ها باید اعتماد کنند؟

**پاسخ:** ElasticNet explainability دارد — می‌توانیم ببینیم کدام features مهم‌ترند. برای CNN، attention maps نشان می‌دهیم. در API، توضیحات ساده می‌دهیم — مثلاً «Concert exit، +۲۲% demand expected». با A/B testing و showing results، trust build می‌کنیم.

**نکته کلیدی:** «ما explainability و human-readable explanations داریم.»

---

### سؤال ۱۵: چطور با Kandoo یکپارچه می‌شود؟

**پاسخ:** Pulsar مکمل Kandoo است، نه جایگزین. Kandoo برای pricing، Pulsar برای driver recommendations. از طریق NATS، events را publish می‌کنیم. ما pricing را تغییر نمی‌دهیم — فقط recommendations می‌دهیم.

**نکته کلیدی:** «Pulsar مکمل Kandoo است. Event-driven integration.»

---

### سؤال ۱۶: اگر راننده‌ها ignore کنند چه می‌شود؟

**پاسخ:** می‌توانیم incentives بدهیم — مثلاً bonus. اگر راننده follow کند و نتیجه مثبت باشد، trust build می‌شود. در آینده، recommendations را personalize می‌کنیم.

**نکته کلیدی:** «ما gamification، feedback loop، و personalization داریم.»

---

### سؤال ۱۷: چه تفاوتی با Uber Movement دارد؟

**پاسخ:** Uber Movement عمومی است. Pulsar مخصوص ride-hailing است — از signals خاص اسنپ استفاده می‌کند. با infrastructure اسنپ یکپارچه است. برای راننده‌ها طراحی شده. MLOps از روز اول.

**نکته کلیدی:** «Pulsar domain-specific و driver-facing است.»

---

### سؤال ۱۸: چرا در ۳ ماه؟ Quality قربانی شده؟

**پاسخ:** روی core pipeline focus کردیم — data، model، API. Core pipeline production-ready است — MLflow، Docker، API. از ساده شروع کردیم — ElasticNet، سپس CNN. Quality metrics داریم — MAE، RMSE، coverage.

**نکته کلیدی:** «ما روی core pipeline focus کردیم. Scope محدود شده، نه quality.»

---

### سؤال ۱۹: اگر تیم دیگری بهتر پیاده‌سازی کند؟

**پاسخ:** Execution مهم است — ما execution کامل داریم. از signals خاص اسنپ استفاده می‌کنیم. با infrastructure اسنپ یکپارچه است — migration cost برای دیگران بالا است. Roadmap داریم — iterating می‌کنیم.

**نکته کلیدی:** «Execution و domain knowledge advantage داریم.»

---

### سؤال ۲۰: آیا مدل fair است؟

**پاسخ:** در MLflow، coverage و distribution را track می‌کنیم. Fairness constraints در roadmap است. می‌توانیم نشان دهیم چرا recommendation داده شده. در production، fairness metrics را monitor می‌کنیم.

**نکته کلیدی:** «ما fairness tracking و monitoring داریم.»

---

### سؤال ۲۱: آیا باعث concentration نمی‌شود؟

**پاسخ:** در recommendations، capacity هر منطقه را در نظر می‌گیریم. Recommendations را diversify می‌کنیم — نه همه به یک منطقه. اگر oversupply شود، model یاد می‌گیرد و adjust می‌کند.

**نکته کلیدی:** «ما capacity constraints و diversification داریم.»

---

## استراتژی پاسخگویی کلی

اگر سوالی پرسیده شد که جوابش را نمی‌دانید:

1. **صادق باشید**: «این سوال خوبی است. من الان جواب دقیق ندارم، اما می‌توانم بعداً follow-up کنم.»

2. **به roadmap اشاره کنید**: «این feature در roadmap است. اول می‌خواستیم core pipeline را validate کنیم.»

3. **Trade-off را توضیح دهید**: «ما trade-off بین X و Y داشتیم. برای MVP، X را انتخاب کردیم.»

4. **Evidence نشان دهید**: «ما این را validate کردیم — در MLflow. می‌توانید ببینید.»

### نکات کلیدی برای همه پاسخ‌ها:

- **صادق باشید**: وعده ندهید، واقعیت بگویید.
- **Evidence نشان دهید**: به MLflow، data، code اشاره کنید.
- **Roadmap داشته باشید**: اگر feature ندارید، بگویید در roadmap است.
- **Trade-off را توضیح دهید**: نشان دهید که تصمیمات شما intentional است.

